---
/**
 * DocsSearch -- Pagefind search UI wrapper for Grove documentation.
 * Delegates to the shared Search component.
 */
import Search from '@/components/Search.astro';

interface Props {
  id?: string;
}

const { id = 'docs-pagefind-search' } = Astro.props;
---

<Search id={id} placeholder="Search docs..." variant="docs" />
